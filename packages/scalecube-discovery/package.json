{
  "name": "@scalecube/scalecube-discovery",
  "version": "0.1.1-alpha.0",
  "private": false,
  "main": "lib/index.js",
  "unpkg": "dist-min/index.js",
  "module": "lib/index.js",
  "types": "lib/index.d.ts",
  "files": [
    "dist-min",
    "dist",
    "lib",
    "es"
  ],
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf node_modules && rm -rf lib && rm -rf es && rm -rf dist && rm -rf dist-min",
    "build": "rm -rf .cache && npm-run-all build:commonJS build:es build:umd build:umd-min",
    "build:commonJS": "rm -rf lib && tsc --module commonjs --target es5 --outDir lib",
    "build:es": "rm -rf es && tsc --module esnext --target es5 --outDir es",
    "build:umd": "rm -rf dist && parcel build src/index.ts --no-minify --experimental-scope-hoisting --out-dir dist --global discovery",
    "build:umd-min": "rm -rf dist-min && parcel build src/index.ts --experimental-scope-hoisting --out-dir dist-min --global discovery",
    "lint": "tslint '{src,tests}/**/*.{ts,tsx}' --fix",
    "prettier": "prettier --write '{src,tests}/**/*.{ts,tsx}'",
    "test": "yarn test-dom-env && yarn test-node-env",
    "test-dom-env": "jest --config jest.config-dom.js",
    "test-node-env": "jest --config jest.config-node.js",
    "coverage": "jest --coverage --collectCoverageFrom='src/**/*.**' --collectCoverageFrom='!tests/**/*.**'",
    "doc": "typedoc ./src/api --out ./doc --mode file --name 'Scalecube Microservice' --hideGenerator --readme ./README.md"
  },
  "author": "Scalecube (https://github.com/scalecube/scalecube-js)",
  "devDependencies": {
    "@types/jest": "^23.3.13",
    "@types/node": "10.1.4",
    "jest": "^23.0.0",
    "parcel-bundler": "^1.11.0",
    "parcel-plugin-bundle-visualiser": "^1.2.0",
    "ts-jest": "^23.10.5",
    "tslint": "^5.11.0",
    "typedoc": "^0.14.2",
    "typescript": "^3.2.4"
  },
  "dependencies": {
    "rxjs": "^6.4.0"
  }
}
